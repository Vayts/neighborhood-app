export interface ITextTooltip {
	direction?: 'right' | 'left' | 'up' | 'down',
	width?: string,
	text: string,
}

export interface ITextTooltipStyle {
	width?: string,
	direction?: 'right' | 'left' | 'up' | 'down',
}
